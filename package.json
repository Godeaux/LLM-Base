{
  "name": "game-foundation",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "echo 'Run: npm install <your-renderer> then configure dev server'",
    "build": "tsc"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
